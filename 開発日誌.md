
## 2026-07-12 Linux環境移行の最終段階・Serving復旧とDBパス・mtime最適化の完了
- **DBパス・mtime最適化**: Windowsから移行された `knowledge.db` のパス形式（バックスラッシュ）をスラッシュに正規化し、ファイルmtimeをLinux環境に合わせて同期するマイグレーションスクリプトを実行。
- **超高速差分コンパイル**: `rebuild_tanuki.py` を実行し、全700ファイル以上のうち新規・変更の数ファイルのみを差分コンパイル。無駄な再埋め込み処理を完全に回避し、高速に `knowledge.bin` を再構築。
- **クライアント接続先最適化**: `TANUKI/.env` に `TANUKI_API_BASE=http://localhost:3000` を追加し、`tanuki_mcp_bridge.py` がローカルの Serving API を参照するように設定を修正。
- **Daemonサービス化**: `tanuki_daemon.py` のローカル動作向け systemd ユーザーサービス定義ファイルを `~/.config/systemd/user/tanuki.service` に配置。
- **tanuki-news 接続・動作最適化**: `models_config.json` および `tanuki-news/.env` の `base_url` / `OLLAMA_URL` を Windows IP から `localhost` に修正し、ローカルの Dashboard API 経由での LLM 呼び出しを復旧。`x_post_editor.py` の Tkinter インポート部分に、Linux環境用の親切なエラー通知ガードを追加。
- **今後の課題**: systemd ユーザーサービスの有効化・起動、および TanukiAIRadioStudio の Linux 移行（g++ビルド調整）。


## 2026-04-30 T.A.N.U.K.I. コンパイラおよびサービングの動作検証
- **コンパイラ**: Gemini API (v1beta) にて gemini-1.5-flash 等のモデル名不一致（404）を確認。以前の生成ログに基づきモデル名を調整したが、Embedding API側で依然として課題あり。
- **サービング**: 	anuki-serving の起動を確認。ReActエージェントがディレクトリに対して 
ead() を試行しエラーになる挙動を観測。エージェントの行動ロジックの改善が必要。
- **環境**: 1 への切り替えテストも実施済み。

## 2026-04-30 T.A.N.U.K.I. 完全ローカル化の完了
- **LLM構成**: すべての処理を Ollama (gemma4:e2b + nomic-embed-text) に移行。Gemini API の温存に成功。
- **コンパイラ**: 埋め込み処理をローカルで実行可能に。Windowsパスのサニタイズ（禁止文字除去）を強化し、Backend のツリー生成エラーを解消。
- **サービング**: エージェントのプロンプトを改善し、ディレクトリへの誤った read() 試行を防止。探索プロセスが安定したことを確認。
- **今後の課題**: 差分更新（Incremental Build）の実装により、大規模ドキュメントへの対応を強化する。

## 2026-05-02 Tanuki Slicer Engine 実装 & 全ノードコンパイル完了
- **Slicer実装**: TanukiParserを活用した汎用スライサー（Python/Markdown）を開発。EBNFベースの論理構造解析により、確実なファイル分割を実現。
- **技術仕様**: チャンク保存タイミングをトークン消費直前に制御するノウハウを SLICER_TECHNICAL_SPEC.md に集約。
- **コンパイル完了**: 全1009ノードの知識コンパイルが完了し、knowledge.db が完成。Serving System はクォータ制限により一時待機中。

